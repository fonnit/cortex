---
milestone: v1.1
milestone_name: Ingest Pipeline Rearchitecture
audited: 2026-04-25
status: tech_debt
scores:
  requirements: 32/32
  phases: 4/4
  integration: 4/4
  flows: 5/5
  toolkit_must_haves: 8/8
  live_operator_runs: 0/5  # Phase 8 — gated to operator
gaps: []
tech_debt:
  - phase: 05-queue-api-surface
    items:
      - "MN-09 (deferred from REVIEW): /api/ingest dedup ignores user_id — tenancy footgun, single-operator MVP defers"
      - "MN-10 (deferred): OWNER_USER_ID resolved at module load — test brittleness only"
      - "MN-11 (deferred): bare !-non-null on DATABASE_URL produces opaque 500 on misconfig"
      - "MN-01 (post-fix): POST /api/classify 4xx skips emit no Langfuse trace (silent on bad payload)"
  - phase: 06-daemon-thin-client
    items:
      - "MN-01: 4xx in HTTP client skips Langfuse trace (silent on bad API key)"
      - "MN-02: LANGFUSE_PUBLIC_KEY non-null assertions silently disable telemetry"
      - "MN-05: /api/ingest manual cast vs discriminatedUnion — works correctly, refactor cosmetic"
      - "Pre-existing modification to agent/src/auth/google.ts uncommitted in working tree (out of phase scope)"
  - phase: 07-stage-1-2-consumers
    items:
      - "MN-01: Stage 2 hot-loops at 5s on taxonomy fetch failure — should back off to 30s"
      - "MN-02: unhandledRejection handler not registered alongside uncaughtException"
      - "MN-03: Stage2ResultSchema.proposed_drive_path accepts empty string"
      - "MN-04: plist PATH ordering inconsistent between consumer and daemon plists"
      - "MN-05: extractFirstJsonObject doesn't track strings before first { (theoretical)"
      - "MN-06: theoretical race on second SIGTERM during shutdown drain"
  - phase: 08-operational-acceptance
    items:
      - "Live operator runs pending — soak (1h), Gmail backfill (6mo), end-to-end visual checkpoint, runtime audits, Langfuse trace walk all need Daniel to run"
deferred_to_v1_2:
  - "Local SQLite fallback queue (currently in-memory cap 100 with overflow-drop-oldest)"
  - "API key rotation tooling"
  - "Identity-context block in classification prompts (v1.0 had it; deleted in Phase 6)"
  - "Per-tenant queue depth limits (single-operator MVP)"
  - "Worker threads / cluster scaling (single-process pools currently sufficient)"
  - "Pre-filter rules on consumer side (rules currently API-side only via existing v1.0 surface)"
  - "Local cache of classification results"
  - "Multi-user support (schema tenancy-ready; product is not)"
preexisting_modifications:
  - file: agent/src/auth/google.ts
    note: "4-line OAuth redirect-URI change from prior session, kept as-is per Phase 6 KEEP-AS-IS constraint. Working-tree-only, not in any v1.1 commit. Reconciliation outside milestone scope."
nyquist:
  enabled: false
  note: "workflow.nyquist_validation: false in config.json — Nyquist discovery skipped"
---

# Milestone v1.1 Audit — Ingest Pipeline Rearchitecture

## Summary

Milestone **v1.1 (Ingest Pipeline Rearchitecture)** is **structurally complete**. All 32 requirements implemented and verified across 4 phases, no critical gaps, no broken flows, no orphaned requirements. Status is **`tech_debt`** rather than `passed` because:

1. **5 ROADMAP success criteria require live operator runs** (Phase 8 by design — soak, Gmail backfill, visual end-to-end check, runtime audits, Langfuse trace walk). The toolkit and runbook are shipped; Daniel runs them and fills in `ACCEPTANCE.md` to close the milestone.
2. **15 deferred minor findings** across phases 5-7 (each previously triaged as non-blocking during code review). Listed for review.
3. **`agent/src/auth/google.ts` working-tree edit** carried over from a prior session — kept as-is per Phase 6's "auth files unchanged" constraint. Needs separate reconciliation commit.

No FAIL-gate triggered. No unsatisfied requirement. Recommend proceeding to `/gsd-complete-milestone v1.1` after Daniel runs the operator acceptance scripts.

## Phase Status (4/4 complete)

| # | Phase | Plans | Tests | Verification | Code Review |
|---|-------|-------|-------|--------------|-------------|
| 5 | Queue & API Surface | 3/3 | 80/80 | passed (10/10 must-haves) | 4 critical + 4 major fixed |
| 6 | Daemon Thin Client | 2/2 | 141/141 | human_needed (13/13 programmatic, 5 live) | 0 critical, 2 major fixed |
| 7 | Stage 1 & 2 Consumers | 2/2 | 290/290 | human_needed (5/5 programmatic, 3 live) | 0 critical, 0 major (clean) |
| 8 | Operational Acceptance | 1/1 | 14/14 | human_needed (8/8 toolkit, 0/5 live by design) | not run (scripts only) |

Total commits this milestone: ~50 across all phases including review fixes and SUMMARY/state writes.

## Requirements Coverage (32/32 ✓)

### Phase 5 — Queue & API Surface (12 requirements)

| REQ-ID | Description | Status | Evidence |
|--------|-------------|--------|----------|
| API-01 | POST /api/ingest with SHA-256 dedup | satisfied | `app/api/ingest/route.ts` |
| API-02 | GET /api/queue?stage=1&limit=N atomic claim | satisfied | `app/api/queue/route.ts` |
| API-03 | GET /api/queue?stage=2&limit=N atomic claim | satisfied | `app/api/queue/route.ts` |
| API-04 | POST /api/classify state-machine | satisfied | `app/api/classify/route.ts` |
| API-05 | CORTEX_API_KEY middleware (401 on miss) | satisfied | `lib/api-key.ts` |
| API-06 | Existing routes unchanged | satisfied | regression test in queue-api-integration.test.ts (12-entry locked snapshot) |
| QUE-01 | Item.status enum extended additively | satisfied | `lib/queue-config.ts` constants |
| QUE-02 | Atomic pending → processing transitions | satisfied | `lib/queue-sql.ts` + `app/api/queue/route.ts` (FOR UPDATE SKIP LOCKED) |
| QUE-03 | Stage 1 retry with hard cap | satisfied | `app/api/classify/route.ts` |
| QUE-04 | Stage 2 retry with hard cap | satisfied | `app/api/classify/route.ts` |
| QUE-05 | Stale-claim reclaim | satisfied | `app/api/queue/route.ts` |
| QUE-06 | No item ends in processing_* | satisfied | distributed invariant verified by stale + retry-cap |

### Phase 6 — Daemon Thin Client (9 requirements)

| REQ-ID | Description | Status | Evidence |
|--------|-------------|--------|----------|
| DAEMON-01 | No Neon access in daemon | satisfied | 16 v1.0 files deleted; no `prisma`/`@neondatabase/serverless` imports under agent/src/ |
| DAEMON-02 | Daemon POSTs file metadata to /api/ingest | satisfied | `agent/src/index.ts` + `agent/src/http/client.ts` |
| DAEMON-03 | Daemon POSTs Gmail metadata to /api/ingest | satisfied | `agent/src/collectors/gmail.ts` |
| DAEMON-04 | Authorization: Bearer ${CORTEX_API_KEY} | satisfied | `agent/src/http/client.ts:postIngest` |
| DAEMON-05 | No classification, no Drive uploads | satisfied | drive.ts and pipeline/* deleted |
| DAEMON-06 | Env surface limited | satisfied | `agent/launchd/com.cortex.daemon.plist` canonical |
| SCAN-01 | Recurse with no depth limit | satisfied | `agent/src/scan.ts:walkDirectory` |
| SCAN-02 | Skip trees containing .git or node_modules | satisfied | scan.ts + downloads.ts skip-prefix runtime guard |
| SCAN-03 | Skip hidden files / dotfiles | satisfied | `agent/src/scan.ts:shouldSkipFile` |

### Phase 7 — Stage 1 & 2 Consumers (6 requirements)

| REQ-ID | Description | Status | Evidence |
|--------|-------------|--------|----------|
| CONS-01 | Stage 1 polls + 10 concurrent | satisfied | `agent/src/consumer/stage1.ts` |
| CONS-02 | Stage 2 polls + 2 concurrent | satisfied | `agent/src/consumer/stage2.ts` |
| CONS-03 | claude -p with file path, never bytes | satisfied | `agent/src/consumer/claude.ts` (execFile) |
| CONS-04 | Gmail metadata prompts; Stage 2 with taxonomy | satisfied | `agent/src/consumer/prompts.ts` |
| CONS-05 | Gmail keep items reach Stage 2 (v1.0 bug fixed) | satisfied | independent worker pools verified by test 10 in consumer-stage2.test.ts |
| CONS-06 | Consumer POSTs result + Langfuse trace | satisfied | trace IDs chained via X-Trace-Id from /api/queue |

### Phase 8 — Operational Acceptance (5 requirements)

| REQ-ID | Description | Status | Evidence |
|--------|-------------|--------|----------|
| ACC-01 | 1h zero-error daemon soak | toolkit-satisfied | `scripts/acc/soak-daemon.sh` + RUNBOOK §G; live run pending |
| ACC-02 | Gmail 6-month backfill completes | toolkit-satisfied | `scripts/acc/gmail-backfill.mjs` + RUNBOOK §C; live run pending |
| ACC-03 | Items flow to triage UI without intervention | toolkit-satisfied | RUNBOOK §E visual checkpoint; live observation pending |
| ACC-04 | Runtime audits — no DATABASE_URL, no argv content | toolkit-satisfied | `audit-daemon-env.sh` + `audit-consumer-argv.sh` + RUNBOOK §B/§D; live run pending |
| ACC-05 | Langfuse trace reconstructs end-to-end | toolkit-satisfied | `audit-langfuse-trace.mjs` + RUNBOOK §F; live run pending |

## Cross-Phase Integration

| From | To | Contract | Verified |
|------|-----|----------|----------|
| Daemon (Ph 6) | API ingest (Ph 5) | POST /api/ingest with Bearer key | http-client tests + heartbeat path test |
| API queue (Ph 5) | Consumer (Ph 7) | GET /api/queue → atomic claim → return items | consumer-stage1/2.test.ts mock the API; integration via pg-mem covers queue SQL |
| Consumer (Ph 7) | API classify (Ph 5) | POST /api/classify → state advance | postClassify 409 short-circuit + state machine tests |
| Consumer (Ph 7) | Taxonomy (Ph 5 ext) | GET /api/taxonomy/internal (requireApiKey) | new route + Stage 2 fresh fetch per batch |
| Phase 8 audit | All phases | Scripts inspect runtime artifacts | dry-runs all exit 0 |

**Wiring observations:**
- Atomic claim (FOR UPDATE SKIP LOCKED) closed against pg-mem integration tests with documented caveats (single-thread)
- Optimistic-concurrency guard added in Phase 5 code review fix prevents the consumer-stale-claim race
- Gmail keep-stuck bug from v1.0 closed by independent Stage 1 / Stage 2 worker pools (test 10 in consumer-stage2.test.ts)
- Langfuse trace chaining via X-Trace-Id header end-to-end

## End-to-End Flows

| Flow | Path | Status |
|------|------|--------|
| Downloads → Triage | chokidar event → daemon POST → ingest → queue → Stage 1 (relevance) → Stage 2 (label) → triage UI | structurally verified by tests; live operator check pending (ACC-03) |
| Gmail → Triage | historyId poll → daemon POST → ingest → queue → Stage 1 → Stage 2 → triage UI | structurally verified; was BROKEN in v1.0 (CONS-05), fixed and verified independent-pool test |
| Failure recovery | Item stuck in processing → stale-claim reclaim → returned to pending → re-claimed | tested in pg-mem (test 14-18 in queue-claim-sql.integration.test.ts) |
| Retry → terminal error | Stage 1/2 fails 5x → status='error' | tested in classify-api error-path tests (5 cases) |
| End-to-end trace | Daemon ingest_post → API ingest → API queue → consumer.classify_cycle → API classify | trace-walker library tests + audit script; live operator check pending (ACC-05) |

## Anti-Pattern Audit (clean)

- ✓ No `DATABASE_URL` in daemon plist or any agent/src/ file
- ✓ No file content as argv to `claude -p` (verified by audit-consumer-argv heuristics)
- ✓ No Clerk on `/api/taxonomy/internal/route.ts` (consumer auth via requireApiKey)
- ✓ No prisma migrations created in v1.1 (CLAUDE.md "Vercel build only" honoured)
- ✓ No new schema columns on Item (queue metadata in classification_trace.queue JSON)
- ✓ No existing routes modified (triage/taxonomy/rules/ask/cron) — verified by 12-entry locked snapshot
- ✓ No SQLite or on-disk persistence in daemon (in-memory FIFO buffer cap 100 + cursor JSON file only)
- ✓ No new top-level deps (only pg-mem@^3 added as devDependency)

## Tech Debt Inventory

15 minor findings deferred during code reviews (full list in `gaps:` section of frontmatter above). Categorized:

- **Observability gaps (3):** Silent 4xx in HTTP client, Langfuse env-var assertions, Stage 2 hot-loop on taxonomy failure
- **Type-safety polish (3):** Manual casts vs discriminatedUnion, empty proposed_drive_path, JSON-string parsing edge cases
- **Operational hardening (3):** unhandledRejection handler, plist PATH consistency, second-SIGTERM race window
- **Test brittleness (1):** OWNER_USER_ID at module load
- **Tenancy concerns (2):** Dedup ignores user_id, opaque 500 on misconfig
- **Pre-existing (1):** auth/google.ts uncommitted edit
- **Phase 8 live runs (5):** ACC-01..05 operator portion

None blocks v1.1 ship. Recommend logging the observability + tenancy items as v1.2 candidates.

## Recommendation

**Proceed to `/gsd-complete-milestone v1.1`** after Daniel completes Phase 8's operator acceptance scripts. The structural milestone is shippable. The 5 live-operator items are by design — Phase 8 ships the toolkit, the operator records observations in `ACCEPTANCE.md`, and that artifact closes the milestone.

If proceeding without waiting for live runs (e.g., to merge the v1.1 branch immediately): the milestone is "code-complete" and can be archived with the explicit note that ACCEPTANCE.md remains pending.

---
*Audit produced by /gsd-audit-milestone (compressed orchestrator path) on 2026-04-25.*
