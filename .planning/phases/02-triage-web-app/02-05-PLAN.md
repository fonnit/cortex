---
phase: 02-triage-web-app
plan: 05
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
  - 02-04
autonomous: false
files_modified:
  - app/(app)/triage/page.tsx
  - components/shell/Sidebar.tsx
  - app/(app)/layout.tsx
requirements:
  - DSN-05

must_haves:
  truths:
    - "The authenticated app loads at /triage with sidebar, metrics strip, and triage queue visible"
    - "Keyboard shortcuts work end-to-end: J/H navigate, K keep, Enter confirm, U undo"
    - "Sidebar queue counts reflect live uncertain item counts from /api/metrics"
    - "Dark mode toggle in sidebar footer switches theme via data-theme on html element"
    - "TypeScript build passes with zero errors"
  artifacts:
    - path: "app/(app)/triage/page.tsx"
      provides: "Triage page wired to Topbar + TriageView"
      exports: ["default"]
    - path: "components/shell/Sidebar.tsx"
      provides: "Dark mode toggle wired to localStorage + data-theme"
      contains: "data-theme"
  key_links:
    - from: "components/shell/Sidebar.tsx"
      to: "document.documentElement"
      via: "dataset.theme toggle"
      pattern: "data-theme"
    - from: "app/(app)/layout.tsx"
      to: "components/shell/MetricsStrip.tsx"
      via: "render in grid row 2"
      pattern: "MetricsStrip"
---

<objective>
Wire all components together, add dark mode toggle, fix any integration issues, and verify the full authenticated flow works end-to-end before the human checkpoint.

Purpose: Integration pass — ensures all Wave 2 plans compose correctly into a working app.
Output: Buildable app with zero TypeScript errors; visual verification ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-triage-web-app/02-01-SUMMARY.md
@.planning/phases/02-triage-web-app/02-02-SUMMARY.md
@.planning/phases/02-triage-web-app/02-03-SUMMARY.md
@.planning/phases/02-triage-web-app/02-04-SUMMARY.md
@design/project/Cortex.html
@design/project/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration wiring + dark mode toggle + TypeScript zero-errors</name>
  <files>components/shell/Sidebar.tsx, app/(app)/layout.tsx, app/layout.tsx</files>
  <read_first>
    - /Users/dfonnegrag/Projects/cortex/design/project/Cortex.html (dark mode: document.documentElement.dataset.theme toggle via tweaks)
    - /Users/dfonnegrag/Projects/cortex/design/project/styles.css ([data-theme="dark"] selector)
    - /Users/dfonnegrag/Projects/cortex/.planning/phases/02-triage-web-app/02-02-SUMMARY.md
    - /Users/dfonnegrag/Projects/cortex/.planning/phases/02-triage-web-app/02-03-SUMMARY.md
  </read_first>
  <action>
    1. **Dark mode toggle in Sidebar footer** (DSN-02): Add a button in `cx-sidebar-foot` that toggles `document.documentElement.dataset.theme` between `'light'` and `'dark'`. Persist to `localStorage.setItem('theme', value)`. Label: "dark" (light mode) / "light" (dark mode) — shows the mode you switch TO. Use `cx-linkbtn` class.

    2. **ThemeScript in root layout**: In `app/layout.tsx`, add an inline `<script>` tag in `<head>` that runs before paint:
       ```html
       <script dangerouslySetInnerHTML={{ __html: `
         try {
           const t = localStorage.getItem('theme') || 'light';
           document.documentElement.dataset.theme = t;
         } catch(e) {}
       `}} />
       ```
       This prevents FOUC. Place before any other head content.

    3. **Integration audit** — read each SUMMARY.md and check for wiring gaps:
       - Verify `MetricsStrip` is imported and rendered in `app/(app)/layout.tsx` (grid row 2)
       - Verify `Sidebar` receives `queues` from `/api/metrics` query in layout
       - Verify `TriageView` is rendered in `app/(app)/triage/page.tsx` with `Topbar`
       - Verify `ReactQueryProvider` wraps the authenticated layout
       - If any component import is missing or a prop is not wired, fix it in this task

    4. **TypeScript zero-errors**: Run `npx tsc --noEmit`. Fix ALL errors in new Phase 2 files. Common issues to pre-empt:
       - `Unsupported("halfvec(512)")` type from Prisma — cast to `unknown` if needed
       - Missing `@types/node` — add if needed
       - `googleapis` types — import `drive_v3` from `googleapis` for typed Drive client

    5. **Build check**: `npm run build` — if build fails, fix errors. Schema changes are applied via `prisma migrate deploy` in build script (already in package.json).

    The goal: `npm run build` succeeds (or at minimum `npx tsc --noEmit` returns zero errors).
  </action>
  <verify>
    <automated>cd /Users/dfonnegrag/Projects/cortex && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "data-theme" /Users/dfonnegrag/Projects/cortex/components/shell/Sidebar.tsx` returns match (dark mode toggle)
    - `grep "localStorage" /Users/dfonnegrag/Projects/cortex/components/shell/Sidebar.tsx` returns match
    - `grep "MetricsStrip" /Users/dfonnegrag/Projects/cortex/app/\(app\)/layout.tsx` returns match
    - `grep "ReactQueryProvider" /Users/dfonnegrag/Projects/cortex/app/\(app\)/layout.tsx` returns match
    - `npx tsc --noEmit` exits 0 with no errors
  </acceptance_criteria>
  <done>All components wired; dark mode toggle works; TypeScript build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual + keyboard verification</name>
  <what-built>
    Complete Phase 2 web app:
    - Clerk auth with sign-in page
    - App shell: sidebar (Cortex logo, 5 nav items, status footer), topbar, 6-cell metrics strip
    - Triage queue: inline-expanding cards, relevance/label modes, all keyboard shortcuts
    - Dark mode toggle
    - Drive resolve cron endpoint
    - Delete API
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Verify redirect to /sign-in (unauthenticated)
    3. Sign in with Clerk — verify MFA flow works
    4. Verify app shell: sidebar visible with Cortex logo + 5 nav items + kbd shortcuts
    5. Verify metrics strip: 6 cells below topbar (values may be "—" if no data yet)
    6. Navigate to /triage — verify queue renders
    7. Test keyboard shortcuts:
       - J/H to navigate between items
       - K to keep (relevance mode item) — verify toast shows "kept"
       - U to undo — verify toast shows "undone"
       - Enter to confirm (label mode item)
    8. Click a collapsed card — verify it expands (TRI-08)
    9. Toggle dark mode via sidebar — verify warm dark palette (#17150f background)
    10. Verify fonts: headings use Newsreader serif, UI uses Inter Tight, data uses JetBrains Mono
    11. Verify cards have NO background and NO border in default mode (TRI-07)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues for fixes</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → DOM | Theme preference read on paint — no sensitive data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | XSS | ThemeScript inline script | accept | Only reads/writes localStorage 'theme' key with known values 'light'/'dark'; no user input involved |
</threat_model>

<verification>
1. `npx tsc --noEmit` — exits 0
2. `grep "data-theme" components/shell/Sidebar.tsx` — present
3. `grep "MetricsStrip" app/(app)/layout.tsx` — present
4. Human checkpoint: visual + keyboard verification approved
</verification>

<success_criteria>
- Zero TypeScript errors
- App loads at /triage after Clerk sign-in
- Sidebar, topbar, metrics strip render with correct design system classes
- Keyboard shortcuts work (J/H, K, U verified by human)
- Dark mode toggle works
- Cards have no background/border in default (content-only per TRI-07)
- Human approves visual checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-triage-web-app/02-05-SUMMARY.md`
</output>
