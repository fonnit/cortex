---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - package.json
  - .env.example
  - agent/package.json
autonomous: true
requirements:
  - ING-03
  - ING-04
  - CLS-03
  - CLS-04
  - CLS-06
  - CLS-07
  - CLS-08
  - DRV-01
  - OBS-06

must_haves:
  truths:
    - "prisma/schema.prisma defines Item, GmailCursor, TaxonomyLabel models with all irreversible columns present (classification_trace JSONB, near_duplicate_of FK, user_id, embedding halfvec(512), proposed_drive_path, drive_inbox_id, axis_* confidence columns)"
    - "npx prisma db push succeeds against the Neon DATABASE_URL"
    - "npx prisma generate succeeds and emits typed client"
    - "The agent/ directory is a separate ESM package (\"type\": \"module\")"
    - "All required env vars are documented in .env.example"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Complete Phase 1 schema — no retroactive migrations needed through Phase 4"
      contains: "model Item"
    - path: "package.json"
      provides: "Web app dependencies with Prisma 7.8.0"
    - path: "agent/package.json"
      provides: "Daemon ESM package with chokidar 5, googleapis, @anthropic-ai/sdk, keytar, langfuse"
    - path: ".env.example"
      provides: "All required env var names"
  key_links:
    - from: "prisma/schema.prisma"
      to: "Neon DATABASE_URL"
      via: "npx prisma db push"
      pattern: "prisma db push"
    - from: "prisma/schema.prisma"
      to: "agent/src/**"
      via: "shared Prisma client"
      pattern: "@prisma/client"

user_setup:
  - service: neon
    why: "Postgres database — required before prisma db push can run"
    env_vars:
      - name: DATABASE_URL
        source: "Neon dashboard → Project → Connection string (pooled, ?sslmode=require)"
  - service: langfuse
    why: "LLM observability — required for daemon to emit traces"
    env_vars:
      - name: LANGFUSE_PUBLIC_KEY
        source: "Langfuse dashboard → Settings → API Keys → Public Key"
      - name: LANGFUSE_SECRET_KEY
        source: "Langfuse dashboard → Settings → API Keys → Secret Key"
  - service: anthropic
    why: "Classification — required for Claude Haiku calls in daemon"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com → API Keys"
  - service: google
    why: "Gmail + Drive API access for daemon"
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client"
---

<objective>
Stand up the complete Neon schema and project package structure that all subsequent plans depend on.

Purpose: Every Phase 1 plan reads from or writes to Neon. The schema contains irreversible columns (classification_trace JSONB, near_duplicate_of FK, embedding halfvec(512), user_id tenancy) that cannot be added cleanly after data exists. This plan makes all of them land in the initial migration.

Output: prisma/schema.prisma (complete, pushed to Neon), web app package.json, agent/package.json (ESM), .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write prisma/schema.prisma with all Phase 1-4 columns</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: Prisma Schema — use verbatim as the base)
    - .planning/research/STACK.md (pgvector halfvec(512), HNSW index, Prisma 7.8.0)
  </read_first>
  <files>prisma/schema.prisma, package.json</files>
  <action>
Create prisma/schema.prisma. Use the schema from Pattern 4 in 01-RESEARCH.md as the direct starting point — do not abbreviate it. Additions beyond that pattern:

1. Add `MetricSnapshot` model for OBS-06 (uncertain_rate + auto_filed_rate tracked daily):
```prisma
model MetricSnapshot {
  id              String   @id @default(cuid())
  user_id         String
  captured_at     DateTime @default(now())
  uncertain_rate  Float    // uncertain items / total items processed that day
  auto_filed_rate Float    // certain items / (certain + uncertain) items that day
  total_ingested  Int
  total_uncertain Int
  total_certain   Int
  total_ignored   Int

  @@index([user_id, captured_at])
}
```

2. Confirm the `Item` model includes every field from Pattern 4 exactly:
- `user_id String` (tenancy)
- `content_hash String @unique`
- `source String` ('downloads' | 'gmail')
- `status String` ('ignored' | 'uncertain' | 'certain' | 'resolved' | 'filed')
- `filename String?`, `mime_type String?`, `size_bytes Int?`, `source_metadata Json?`
- `classification_trace Json?`
- `proposed_drive_path String?`, `confirmed_drive_path String?`
- `drive_inbox_id String?`, `drive_filed_id String?`
- `axis_type String?`, `axis_from String?`, `axis_context String?`
- `axis_type_confidence Float?`, `axis_from_confidence Float?`, `axis_context_confidence Float?`
- `near_duplicate_of String?` with self-relation
- `embedding Unsupported("halfvec(512)")?`
- `ingested_at DateTime @default(now())`, `updated_at DateTime @updatedAt`
- `@@index([user_id, status])`, `@@index([content_hash])`

3. Confirm `GmailCursor` model: `id, user_id @unique, last_history_id String?, last_successful_poll_at DateTime?, updated_at @updatedAt`

4. Confirm `TaxonomyLabel` model: `id, user_id, axis String, name String, item_count Int @default(0), last_used DateTime?, deprecated Boolean @default(false), created_at DateTime @default(now()), @@unique([user_id, axis, name]), @@index([user_id, axis])`

Generator block: `provider = "prisma-client-js"`, `previewFeatures = ["postgresqlExtensions"]`
Datasource: `provider = "postgresql"`, `url = env("DATABASE_URL")`, `extensions = [pgvector(map: "vector", schema: "public")]`

After writing schema.prisma, initialize package.json if not present:
```json
{
  "name": "cortex",
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "prisma generate && prisma migrate deploy && next build",
    "start": "next start",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "next": "16.2.4",
    "react": "19.2.5",
    "react-dom": "19.2.5",
    "typescript": "6.0.3",
    "tailwindcss": "4.2.4",
    "@tanstack/react-query": "5.100.1",
    "zod": "4.3.6",
    "@clerk/nextjs": "7.2.5",
    "prisma": "7.8.0",
    "@prisma/adapter-neon": "7.8.0",
    "@neondatabase/serverless": "1.1.0",
    "langfuse": "3.38.20",
    "openai": "6.34.0"
  }
}
```

Run: `npm install` in the cortex root.
  </action>
  <verify>
    <automated>grep -c "model Item\|model GmailCursor\|model TaxonomyLabel\|model MetricSnapshot" /Users/dfonnegrag/Projects/cortex/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
- `grep "model Item" prisma/schema.prisma` returns a match
- `grep "halfvec(512)" prisma/schema.prisma` returns a match
- `grep "classification_trace" prisma/schema.prisma` returns a match
- `grep "near_duplicate_of" prisma/schema.prisma` returns a match
- `grep "MetricSnapshot" prisma/schema.prisma` returns a match
- `grep "uncertain_rate" prisma/schema.prisma` returns a match
- `cat package.json | grep '"prisma": "7.8.0"'` returns a match
  </acceptance_criteria>
  <done>schema.prisma contains all 4 models with irreversible columns; package.json exists with Prisma 7.8.0.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize agent ESM package + push schema to Neon</name>
  <read_first>
    - prisma/schema.prisma (just written — verify all 4 models are present)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 5: launchd plist; resolved open questions for keytar and chokidar v5 ESM)
  </read_first>
  <files>agent/package.json, agent/tsconfig.json, .env.example</files>
  <action>
Create agent/package.json as a separate ESM package (chokidar v5 requires this):
```json
{
  "name": "cortex-agent",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "node --watch dist/index.js"
  },
  "dependencies": {
    "chokidar": "5.0.0",
    "googleapis": "171.4.0",
    "@anthropic-ai/sdk": "0.91.0",
    "keytar": "7.9.0",
    "langfuse": "3.38.20",
    "@neondatabase/serverless": "1.1.0",
    "@prisma/client": "7.8.0"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "@types/node": "22.x"
  }
}
```

Create agent/tsconfig.json:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "outDir": "dist",
    "rootDir": "src",
    "esModuleInterop": true,
    "skipLibCheck": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

Create .env.example with ALL required env vars:
```
# Neon Postgres
DATABASE_URL=postgresql://...?sslmode=require

# Anthropic (classification)
ANTHROPIC_API_KEY=sk-ant-...

# Langfuse (observability)
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_HOST=https://cloud.langfuse.com

# Google OAuth (Gmail + Drive — stored in Keychain at runtime, needed for initial auth)
GOOGLE_CLIENT_ID=...apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-...

# Clerk (web app — Phase 2, document now)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_...
CLERK_SECRET_KEY=sk_...
```

Run `npm install` inside agent/ to verify all packages resolve.

[BLOCKING] Run schema push:
```bash
cd /Users/dfonnegrag/Projects/cortex && npx prisma db push
```
This requires DATABASE_URL to be set in the environment. If it is not set, the task will fail with a clear error. Do not proceed past this step until push succeeds — all subsequent plans depend on the Neon schema existing.
  </action>
  <verify>
    <automated>cd /Users/dfonnegrag/Projects/cortex && npx prisma validate && echo "Schema valid"</automated>
  </verify>
  <acceptance_criteria>
- `cat agent/package.json | grep '"type": "module"'` returns a match
- `cat agent/package.json | grep '"chokidar": "5.0.0"'` returns a match
- `cat agent/package.json | grep '"keytar": "7.9.0"'` returns a match
- `cat .env.example | grep DATABASE_URL` returns a match
- `npx prisma validate` exits 0
- `npx prisma db push` exits 0 (schema live in Neon)
  </acceptance_criteria>
  <done>agent/ is an ESM package; .env.example documents all secrets; schema is live in Neon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars → Prisma | DATABASE_URL must not be committed; .env.example only |
| schema.prisma → Neon | Push only runs with explicit DATABASE_URL; no auto-migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env / credentials | mitigate | .env.example only committed; .env in .gitignore; keytar for runtime secrets |
| T-01-02 | Tampering | prisma/schema.prisma | accept | Single operator; schema is source-controlled; push requires explicit DATABASE_URL |
| T-01-03 | Information Disclosure | DATABASE_URL in env | mitigate | Neon connection string uses SSL (sslmode=require); never logged |
</threat_model>

<verification>
After both tasks complete:
1. `npx prisma validate` passes
2. `npx prisma db push` exits 0 (or was already run)
3. `grep -c "model " prisma/schema.prisma` returns 4
4. `cat agent/package.json | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['type']=='module'"` passes
5. `.env.example` lists all 6 env var groups
</verification>

<success_criteria>
- Neon schema is live with all 4 models and all irreversible columns
- agent/ is a valid ESM package that installs cleanly
- .env.example documents every secret needed through Phase 4
- No schema migration will be required in Phases 2-4 for columns defined here
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` using the summary template.
</output>
