---
quick_id: 260430-g6h
type: execute
wave: 1
depends_on: []
files_modified:
  # Backend (Task 1)
  - agent/src/consumer/prompts.ts
  - agent/src/consumer/stage2.ts
  - agent/src/http/types.ts
  - agent/src/mcp/cortex-tools.ts
  - app/api/classify/route.ts
  - app/api/triage/route.ts
  - app/api/labels/samples/route.ts
  - app/api/taxonomy/route.ts
  - app/api/taxonomy/[axis]/[name]/route.ts
  - app/api/taxonomy/merge/route.ts
  - app/api/taxonomy/check-duplicate/route.ts
  - app/api/taxonomy/internal/route.ts
  - app/api/cron/embed/route.ts
  - app/api/ask/route.ts
  - lib/embed.ts
  # UI (Task 2)
  - components/triage/TriageView.tsx
  - components/triage/ExpandedCard.tsx
  - app/(app)/taxonomy/page.tsx
  # Tests (Task 3)
  - __tests__/classify-api.test.ts
  - __tests__/classify-auto-actions.test.ts
  - __tests__/triage-api.test.ts
  - __tests__/labels-samples-api.test.ts
  - __tests__/taxonomy-internal-api.test.ts
  - agent/__tests__/consumer-prompts.test.ts
  - agent/__tests__/consumer-stage2-prompt.test.ts
  - agent/__tests__/consumer-stage2.test.ts
autonomous: true
requirements:
  - SEED-v4-D1  # SEED-v4-prod.md Decision 1: drop axis_context throughout runtime + UI
must_haves:
  truths:
    - "Stage 2 prompt mentions only 'Type axis' and 'From axis' — no 'Context axis' line, no instruction to call cortex_label_samples on a context label."
    - "Stage 2 model output is parsed against a Zod schema whose `axes` shape is exactly { type, from } — sending a payload with `axes.context` is rejected as parse_error."
    - "MCP `cortex_label_samples` tool input rejects axis='context' (not in the enum)."
    - "POST /api/classify on stage=2 success with valid type+from axes (and no context axis present) succeeds and writes axis_type + axis_from + their confidences. The Item row's axis_context column is never written and remains null on insert."
    - "POST /api/triage with picks containing only Type and From files the item; the Context pick is no longer accepted at the API boundary."
    - "GET /api/triage list response items omit any axis_context value derivation (no Context proposals or confidence in stage2 normalised trace)."
    - "GET /api/labels/samples?axis=context&label=anything returns 400 (Bad Request)."
    - "PATCH /api/taxonomy/context/<name> returns 400 (invalid axis)."
    - "POST /api/taxonomy with { axis: 'context', name } returns 400 (Zod validation_failed)."
    - "POST /api/taxonomy/merge with { axis: 'context', ... } returns 400 (Zod validation_failed)."
    - "GET /api/taxonomy response object no longer contains a `contexts` array."
    - "GET /api/taxonomy/internal response object no longer contains a `context` array (Stage 2 worker fetches taxonomy with shape { type, from } only)."
    - "Triage UI ExpandedCard shows two axis rows (Type, From) — the Context row is gone. Tab order on the row stays Type → From → drive_path preview."
    - "Taxonomy page tabs are exactly Types and Entities — no 'Contexts' tab. Tab state type is `'types' | 'entities'`."
    - "buildEmbedText composes from filename + axis_type + axis_from + subject — no axis_context line."
    - "/api/ask raw-SQL SELECT no longer projects axis_context (drops it from the projection list)."
    - "`npx tsc --noEmit` exits 0."
    - "`npx jest` exits 0 (modulo pre-existing failures unrelated to context-axis)."
    - "`grep -rn \"axis_context\\|axis: 'context'\\|\\\"contexts\\\"\" app/ agent/src/ components/ lib/` returns matches ONLY in prisma/schema.prisma (not in app/agent/src/components/lib code)."
  artifacts:
    - path: agent/src/consumer/prompts.ts
      provides: Stage 2 prompt without context-axis line + TaxonomyContext shape { type, from }
      contains: "buildStage2Prompt"
    - path: agent/src/consumer/stage2.ts
      provides: Stage2ResultSchema with axes={type,from} and matching forwarded ClassifyRequest
      contains: "Stage2ResultSchema"
    - path: agent/src/http/types.ts
      provides: TaxonomyInternalResponse + ClassifyRequest typed without context
      contains: "TaxonomyInternalResponse"
    - path: agent/src/mcp/cortex-tools.ts
      provides: cortex_label_samples Zod input axis: z.enum(['type','from'])
      contains: "z.enum(['type', 'from'])"
    - path: app/api/classify/route.ts
      provides: ClassifyBodySchema with axes.context dropped + auto-file gates compute on 2 axes only + no axis_context write path
      contains: "axes: z.object({ type: AxisSchema, from: AxisSchema })"
    - path: app/api/triage/route.ts
      provides: GET buildProposals/normaliser ignores axis_context; POST DecisionSchema picks={Type,From}; upsertLabel only fires for Type and From
    - path: app/api/labels/samples/route.ts
      provides: ALLOWED_AXES = ['type','from']
      contains: "ALLOWED_AXES = ['type', 'from']"
    - path: app/api/taxonomy/route.ts
      provides: GET response { types, entities, mergeProposals }; POST Zod axis enum ['type','from']
    - path: app/api/taxonomy/[axis]/[name]/route.ts
      provides: AXIS_COL with only type+from keys
    - path: app/api/taxonomy/merge/route.ts
      provides: MergeBody axis enum ['type','from']
    - path: app/api/taxonomy/check-duplicate/route.ts
      provides: validates axis ∈ {'type','from'} (returns 400 for 'context')
    - path: app/api/taxonomy/internal/route.ts
      provides: GET response { type, from } only
      contains: "Response.json"
    - path: app/api/cron/embed/route.ts
      provides: SELECT projection drops axis_context; buildEmbedText fed without that field
    - path: app/api/ask/route.ts
      provides: raw SQL SELECT projection drops axis_context; TS row type drops axis_context
    - path: lib/embed.ts
      provides: EmbedItem interface drops axis_context; buildEmbedText composes from filename+type+from+subject only
    - path: components/triage/TriageView.tsx
      provides: keyboard handler axes = ['Type','From']; mutation picks shape { Type, From }; trace.confident normalisation unchanged but axes loop has 2 entries
    - path: components/triage/ExpandedCard.tsx
      provides: axes = isLabel ? ['Type','From'] : []; TriageItem type drops context branch in proposals
    - path: app/(app)/taxonomy/page.tsx
      provides: tab state `'types' | 'entities'`; tabs array has 2 entries; tabAxis map has 2 entries; TaxonomyData shape drops contexts
  key_links:
    - from: agent/src/consumer/prompts.ts (buildStage2Prompt)
      to: agent/src/consumer/stage2.ts (Stage2ResultSchema)
      via: same JSON shape — prompt asks for axes={type,from}, schema parses axes={type,from}
    - from: agent/src/consumer/stage2.ts (forwarded ClassifyRequest)
      to: app/api/classify/route.ts (ClassifyBodySchema)
      via: HTTP POST /api/classify; both shapes must drop axes.context together (any drift = parse_error / 400)
    - from: app/api/classify/route.ts
      to: prisma.item.update updateMany data block
      via: never set updateData.axis_context (column stays null)
    - from: components/triage/ExpandedCard.tsx onAction
      to: app/api/triage/route.ts POST DecisionSchema picks
      via: picks shape { Type, From } — Context key never sent / never accepted
    - from: app/(app)/taxonomy/page.tsx (tab state)
      to: app/api/taxonomy/route.ts GET response
      via: GET returns { types, entities, mergeProposals }; tab state union narrowed to those two keys
    - from: agent/src/mcp/cortex-tools.ts cortex_label_samples
      to: app/api/labels/samples/route.ts ALLOWED_AXES
      via: MCP enum and route allow-list both drop 'context' together — keep them in sync to avoid the model asking for an axis the route refuses
---

<objective>
Finish SEED-v4-prod.md Decision 1: strip `axis_context` from every code path that
reads, writes, prompts for, validates, or renders it. The schema column stays
(no Prisma migration); old rows keep their values; new rows write null on the
axis_context column. The agent prompt no longer mentions a Context axis; the
Zod contract between the agent and `/api/classify` rejects payloads carrying
an `axes.context` field; the triage card and the taxonomy page no longer show
or accept Context as an option.

Purpose: the v4 seed already stops seeding context labels (260430-0ff). Until
this strip lands, Stage 2 still elicits a context value from the model
(verified — model returned `axis_context=travel` even with an empty seed),
which (a) grows TaxonomyLabel rows nobody asked for, (b) wastes prompt tokens,
(c) forces a fourth axis decision when 3 (type, from, path) are sufficient, and
(d) shows a useless Context input row in triage.

Output: one atomic backend commit, one atomic UI commit, one atomic test-fix
commit. `npx tsc --noEmit` clean; `npx jest` green modulo the pre-existing
failures noted in STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-quick.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260427-tlk-base-taxonomy-seed/SEED-v4-prod.md
@.planning/STATE.md
@./CLAUDE.md
@agent/src/consumer/prompts.ts
@agent/src/consumer/stage2.ts
@agent/src/http/types.ts
@agent/src/mcp/cortex-tools.ts
@app/api/classify/route.ts
@app/api/triage/route.ts
@app/api/labels/samples/route.ts
@app/api/taxonomy/route.ts
@app/api/taxonomy/[axis]/[name]/route.ts
@app/api/taxonomy/merge/route.ts
@app/api/taxonomy/check-duplicate/route.ts
@app/api/taxonomy/internal/route.ts
@app/api/cron/embed/route.ts
@app/api/ask/route.ts
@lib/embed.ts
@components/triage/TriageView.tsx
@components/triage/ExpandedCard.tsx
@components/triage/AxisGroup.tsx
@app/(app)/taxonomy/page.tsx
@prisma/schema.prisma

<interfaces>
<!-- Reference shapes: drop the `context` field from each. The schema column stays. -->

agent/src/consumer/prompts.ts (current):
```ts
export interface TaxonomyContext {
  type: string[]
  from: string[]
  context: string[]   // ← REMOVE
}
```

agent/src/consumer/stage2.ts (current Zod):
```ts
const Stage2ResultSchema = z.object({
  axes: z.object({ type: AxisSchema, from: AxisSchema, context: AxisSchema }),  // ← drop context
  ...
})
```

agent/src/http/types.ts (current):
```ts
export interface TaxonomyInternalResponse {
  type: string[]
  from: string[]
  context: string[]   // ← REMOVE
}
export type ClassifyRequest = | {
  ...
  axes?: { type: ClassifyAxis; from: ClassifyAxis; context: ClassifyAxis }   // ← drop context
  ...
}
```

agent/src/mcp/cortex-tools.ts (current):
```ts
{ axis: z.enum(['type', 'from', 'context']), ... }   // ← drop 'context'
```

app/api/classify/route.ts (current ClassifyBodySchema):
```ts
axes: z.object({ type: AxisSchema, from: AxisSchema, context: AxisSchema }).optional(),  // ← drop context
// auto-file gate currently:  tConf>=0.85 && fConf>=0.85 && cConf>=0.85
//                              && tAxis.value && fAxis.value && cAxis.value
// → after strip:               tConf>=0.85 && fConf>=0.85
//                              && tAxis.value && fAxis.value
// auto-ignore confidence fallback: max(tConf, fConf, cConf) → max(tConf, fConf)
// updateData.axis_context line REMOVED; axis_context_confidence line REMOVED
```

app/api/triage/route.ts:
```ts
DecisionSchema.picks: { Type?, From?, Context? }   // ← drop Context
upsertLabel('context', picks.Context) line REMOVED
data.axis_context = picks.Context line REMOVED
buildProposals: axes object loops over both type and from only
```

app/api/labels/samples/route.ts:
```ts
const ALLOWED_AXES = ['type', 'from', 'context'] as const   // ← drop 'context'
// switch on axis: drop case 'context'
// select: drop axis_context: true
```

app/api/taxonomy/route.ts:
```ts
GET response: { types, entities, mergeProposals }   // ← drop contexts
POST: z.enum(['type','from','context']) → z.enum(['type','from'])
```

app/api/taxonomy/[axis]/[name]/route.ts:
```ts
const AXIS_COL = { type: 'axis_type', from: 'axis_from' }   // drop context
```

app/api/taxonomy/merge/route.ts:
```ts
MergeBody axis: z.enum(['type','from','context']) → z.enum(['type','from'])
const AXIS_COL = { type: 'axis_type', from: 'axis_from' }   // drop context
```

app/api/taxonomy/check-duplicate/route.ts:
```ts
// add explicit axis whitelist guard so axis='context' returns 400
```

app/api/taxonomy/internal/route.ts:
```ts
GET response: { type, from }   // drop context
```

app/api/cron/embed/route.ts:
```ts
select: { axis_context: true, ... }   // ← drop axis_context: true
// buildEmbedText is fed an item without axis_context
```

app/api/ask/route.ts:
```ts
prisma.$queryRaw<...>: drop axis_context from row type and SELECT list
docLines: no change needed (didn't reference axis_context)
```

lib/embed.ts:
```ts
interface EmbedItem { axis_context?: string | null }   // ← REMOVE
buildEmbedText: drop item.axis_context from parts array
```

components/triage/ExpandedCard.tsx:
```ts
// TriageItem.classification_trace.stage2.proposals: drop `context?` branch
const axes = isLabel ? ['Type', 'From', 'Context'] : []   // → ['Type', 'From']
```

components/triage/TriageView.tsx:
```ts
TriageDecision.picks: { Type?, From?, Context? }   // ← drop Context
keyboard handler:
  const axes = ['Type', 'From', 'Context']  // → ['Type', 'From']
  ['1', '2', '3'].includes(k) → ['1', '2'].includes(k)
mutation picks build: drop Context: a.picks['Context']
```

app/(app)/taxonomy/page.tsx:
```ts
interface TaxonomyData { types, entities, contexts, mergeProposals }   // ← drop contexts
const [tab, setTab] = useState<'types' | 'entities' | 'contexts'>('types')
  → useState<'types' | 'entities'>('types')
const tabs = [ types, entities, contexts ]   // ← drop contexts
const tabAxis = { types: 'type', entities: 'from', contexts: 'context' }   // drop contexts
```

prisma/schema.prisma — DO NOT TOUCH. axis_context + axis_context_confidence
columns stay; old data preserved; new inserts default null.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip axis_context from all backend code (prompt, Zod, API routes, MCP server, embedding builder)</name>
  <files>
    agent/src/consumer/prompts.ts,
    agent/src/consumer/stage2.ts,
    agent/src/http/types.ts,
    agent/src/mcp/cortex-tools.ts,
    app/api/classify/route.ts,
    app/api/triage/route.ts,
    app/api/labels/samples/route.ts,
    app/api/taxonomy/route.ts,
    app/api/taxonomy/[axis]/[name]/route.ts,
    app/api/taxonomy/merge/route.ts,
    app/api/taxonomy/check-duplicate/route.ts,
    app/api/taxonomy/internal/route.ts,
    app/api/cron/embed/route.ts,
    app/api/ask/route.ts,
    lib/embed.ts
  </files>
  <action>
    Per SEED-v4-prod.md Decision 1, drop `axis_context` from runtime everywhere except the prisma schema column itself.

    **agent/src/consumer/prompts.ts**:
    - `TaxonomyContext`: remove `context: string[]` field.
    - `buildStage2Prompt`:
      - Remove the `Context axis: ${listOrNoneYet(taxonomy.context)}` line (line 114).
      - In the JSON shape sentence at the bottom, change
        `"axes": {"type":..., "from":{...}, "context":{...}}` to
        `"axes": {"type":..., "from":{...}}`.
      - In the MANDATORY tool-calls block, the third bullet "For each axis where you are reusing an existing label..." stays but applies to two axes only — no change to wording needed because it iterates "each axis"; just verify the prompt is consistent (it now references only Type and From in the taxonomy snapshot).

    **agent/src/consumer/stage2.ts**:
    - `Stage2ResultSchema.axes`: change `z.object({ type: AxisSchema, from: AxisSchema, context: AxisSchema })` to `z.object({ type: AxisSchema, from: AxisSchema })`.
    - In `handleOne`, when normalising `axes` for the success payload: drop the `context: { value: ..., confidence: ... }` block from both the local `axes` const and the `payload.axes` field.
    - In `buildStage2Prompt(item, { type: ..., from: ..., context: ... })` call: drop `context: taxonomy.context`.
    - Update the file-level docstring's "all-3-axes contract" wording to "two-axes contract (type, from)" so the comment matches the code.

    **agent/src/http/types.ts**:
    - `TaxonomyInternalResponse`: drop the `context: string[]` field.
    - `ClassifyRequest` success branch: change `axes?.context` field out of the inline shape (so it's `axes?: { type: ClassifyAxis; from: ClassifyAxis }`).
    - Update both jsdoc blocks that say "all-three-axes contract" / "All 3 axis confidences" to "two axes (type, from)".

    **agent/src/mcp/cortex-tools.ts**:
    - In the `cortex_label_samples` tool registration, change `axis: z.enum(['type', 'from', 'context'])` to `axis: z.enum(['type', 'from'])`.
    - Update the tool description string ("axis must be one of 'type'|'from'|'context'") to drop `|'context'`.

    **app/api/classify/route.ts**:
    - `ClassifyBodySchema` success branch `axes`: change to `z.object({ type: AxisSchema, from: AxisSchema }).optional()`.
    - Stage 2 success path:
      - Drop `const cAxis = data.axes.context` and `const cConf = cAxis.confidence`.
      - `allConfident`: `tConf >= CONFIDENCE_THRESHOLD && fConf >= CONFIDENCE_THRESHOLD` (drop cConf).
      - `allHighConf`: `tConf >= AUTO_FILE_THRESHOLD && fConf >= AUTO_FILE_THRESHOLD`.
      - `allValuesPresent`: `tAxis.value !== null && fAxis.value !== null`.
      - `ignoreConfidence` fallback: `data.confidence ?? Math.max(tConf, fConf)`.
      - Auto-ignore-skip branch: drop `if (cAxis.value) updateData.axis_context = cAxis.value` and `updateData.axis_context_confidence = cConf`. The auto-ignore branch never wrote axis_*; no change there.
      - In the writes-axis branch: keep writing `axis_type`, `axis_from`, `axis_type_confidence`, `axis_from_confidence` only. Do NOT write `axis_context` or `axis_context_confidence` — those columns stay null on insert / unchanged on update.
    - Update the doc comments above the auto-file gate ("All 3 axis confidences ≥ AUTO_FILE_THRESHOLD" → "Both axis confidences ≥ AUTO_FILE_THRESHOLD"; "All 3 axis values are non-null" → "Both axis values are non-null").

    **app/api/triage/route.ts**:
    - `buildProposals`: change the input type signature so `axis_context` and `axis_context_confidence` fields are removed from the parameter type. The function loops over `Object.entries(axes)` so behaviour follows the upstream stage2 trace shape — no further code change needed (Stage 2 no longer emits `axes.context`).
    - `DecisionSchema.picks`: drop the `Context: z.string().optional()` field.
    - In the `archive` / `confirm` branch:
      - Drop `if (picks?.Context) data.axis_context = picks.Context`.
      - Drop `if (picks?.Context) labelOps.push(upsertLabel('context', picks.Context))`.
    - GET response: serialised `Item` rows still include `axis_context` (Prisma default-selects all columns). Add an explicit `select` or strip the field from the response so the wire payload doesn't carry the orphaned column. Implementation: replace `prisma.item.findMany({ where, orderBy, take })` with `prisma.item.findMany({ where, orderBy, take, select: { /* every column except axis_context, axis_context_confidence */ } })`. Enumerate every Item column we currently return (id, user_id, source, content_hash, filename, mime_type, size_bytes, ingested_at, status, axis_type, axis_from, axis_type_confidence, axis_from_confidence, classification_trace, proposed_drive_path, confirmed_drive_path, source_metadata, embedding — verify against schema.prisma). If listing every column is fragile, the simpler alternative is: keep findMany default, and after mapping strip `axis_context` and `axis_context_confidence` from the spread (`const { axis_context: _, axis_context_confidence: __, ...rest } = item; return { ...rest, classification_trace: ..., stage }`). Use the spread-strip approach.

    **app/api/labels/samples/route.ts**:
    - `ALLOWED_AXES`: change `['type', 'from', 'context'] as const` to `['type', 'from'] as const`.
    - Drop the `case 'context'` branch from the where-builder switch.
    - Drop `axis_context: true` from the `select` object.
    - Effect: `?axis=context&label=anything` now hits the whitelist guard → 400.

    **app/api/taxonomy/route.ts**:
    - GET: drop the `contexts = labels.filter(l => l.axis === 'context')...` block. The response shape becomes `{ types, entities, mergeProposals }` (no `contexts` key).
    - POST: change `axis: z.enum(['type', 'from', 'context'])` → `z.enum(['type', 'from'])`.

    **app/api/taxonomy/[axis]/[name]/route.ts**:
    - `AXIS_COL`: drop the `context: 'axis_context'` entry. The existing `if (!axisCol) return 400` guard handles the rejection automatically.

    **app/api/taxonomy/merge/route.ts**:
    - `MergeBody.axis`: change `z.enum(['type', 'from', 'context'])` → `z.enum(['type', 'from'])`.
    - `AXIS_COL`: drop the `context` entry.

    **app/api/taxonomy/check-duplicate/route.ts**:
    - Add an explicit axis whitelist guard immediately after parsing query params:
      ```ts
      if (axis !== 'type' && axis !== 'from') {
        return Response.json({ error: 'invalid axis' }, { status: 400 })
      }
      ```
      so `?axis=context&name=anything` returns 400 instead of running the (now-empty) similarity loop on context-axis labels.

    **app/api/taxonomy/internal/route.ts**:
    - Drop the `const context = labels.filter(...)` line.
    - Change `Response.json({ type, from, context }, ...)` to `Response.json({ type, from }, ...)`.

    **app/api/cron/embed/route.ts**:
    - In the `prisma.item.findMany` `select`: drop `axis_context: true`.
    - `buildEmbedText` reads `item.axis_context` only if present; once `EmbedItem` type drops the field (next), this stays type-clean.

    **app/api/ask/route.ts**:
    - Drop `axis_context` from the inline TS row type for `prisma.$queryRaw<...>`.
    - Drop `axis_context` from the `SELECT id, filename, axis_type, axis_from, axis_context, ...` projection list (becomes `SELECT id, filename, axis_type, axis_from, confirmed_drive_path, proposed_drive_path, ingested_at, source_metadata FROM "Item" ...`).
    - The downstream `docLines` builder doesn't reference axis_context — no change there.

    **lib/embed.ts**:
    - `EmbedItem`: drop `axis_context?: string | null`.
    - `buildEmbedText`: drop `item.axis_context` from the `parts` array (becomes `[item.filename, item.axis_type, item.axis_from, subject]`).

    **DO NOT touch prisma/schema.prisma** — the `axis_context` and `axis_context_confidence` columns stay (per spec). New inserts get null on those columns automatically (no DEFAULT was added; Prisma writes only the fields named in `data`).

    **DO NOT touch scripts/apply-content-pass.ts, scripts/process-files.ts, scripts/smoke-triage-fix.ts** — those are out-of-scope ad-hoc scripts (Daniel's verification path; they may continue to reference axis_context to sanity-check old rows).

    Run `rg "axis_context|axis: 'context'|\"contexts\"" app/ agent/src/ components/ lib/` after changes — expected output: empty (zero matches in scope).
  </action>
  <verify>
    <automated>npx tsc --noEmit && cd agent && npx tsc --noEmit && cd .. && rg --type ts "axis_context|axis: 'context'" app/ agent/src/ components/ lib/ && echo 'EXPECTED: rg returned matches → fail; check for stragglers' || echo 'OK: rg empty'</automated>
  </verify>
  <done>
    `npx tsc --noEmit` and `(cd agent && npx tsc --noEmit)` both exit 0.
    rg in app/ agent/src/ components/ lib/ for axis_context / axis: 'context' / "contexts" returns zero matches.
    The Stage 2 prompt rendered for an item no longer contains "Context axis".
    The Zod schemas in classify, taxonomy, taxonomy/merge, mcp cortex-tools, Stage2ResultSchema all reject the `context` axis.
  </done>
</task>

<task type="auto">
  <name>Task 2: Strip axis_context from triage UI + taxonomy page</name>
  <files>
    components/triage/TriageView.tsx,
    components/triage/ExpandedCard.tsx,
    app/(app)/taxonomy/page.tsx
  </files>
  <action>
    **components/triage/ExpandedCard.tsx**:
    - `TriageItem.classification_trace.stage2.proposals` shape: drop the `context?: Array<{ value, conf }>` field. Result:
      ```ts
      proposals?: {
        type?: Array<{ value: string; conf: number }>
        from?: Array<{ value: string; conf: number }>
      }
      ```
    - In the component body: change
      `const axes = isLabel ? ['Type', 'From', 'Context'] : []`
      to
      `const axes = isLabel ? ['Type', 'From'] : []`.
    - The `axes.map((a) => <AxisGroup ... />)` loop now renders 2 rows. The proposed_drive_path block stays as the third row. Tab order naturally flows Type → From → drive_path preview area.
    - `AxisGroup.tsx`: no changes needed — it's axis-agnostic. The `proposals` prop indexing `proposals?.[a.toLowerCase() as keyof typeof proposals]` will type-check against the narrowed proposals object once ExpandedCard's TriageItem type is updated.

    **components/triage/TriageView.tsx**:
    - `TriageDecision.picks`: drop `Context?: string`. Result:
      ```ts
      picks?: { Type?: string; From?: string }
      ```
    - In the keyboard handler:
      - `const axes = ['Type', 'From', 'Context']` (lines 211, 222) → `['Type', 'From']` (BOTH occurrences).
      - `['1', '2', '3'].includes(k)` (line 210) → `['1', '2'].includes(k)`.
      - `const axisKey = unresolved.toLowerCase() as 'type' | 'from' | 'context'` (line 217) → `as 'type' | 'from'`.
    - In the `mutation.mutate(...)` call where picks are forwarded (lines 159–164): drop `Context: a.picks['Context']`. Result:
      ```ts
      picks: a.picks ? { Type: a.picks['Type'], From: a.picks['From'] } : undefined
      ```

    **app/(app)/taxonomy/page.tsx**:
    - `TaxonomyData` interface: drop `contexts: TaxonomyRow[]`.
    - `placeholderData`: drop `contexts: []` from the placeholder object.
    - Tab state: change `useState<'types' | 'entities' | 'contexts'>('types')` to `useState<'types' | 'entities'>('types')`.
    - `tabs` array: drop the `{ id: 'contexts' as const, label: 'Contexts' }` entry.
    - `tabAxis` map: drop the `contexts: 'context'` entry. Result:
      ```ts
      const tabAxis: Record<typeof tab, string> = {
        types: 'type',
        entities: 'from',
      }
      ```
    - `data?.[tab]` indexing keeps working — `tab` is now narrowed to `'types' | 'entities'` and `TaxonomyData` has both keys.
    - Modal handlers (rename / merge / split / deprecate) all use `modal.axis` (already narrowed by `tabAxis[tab]`) — no further change.

    Visual reflow check: ExpandedCard goes from 3 axis rows + path → 2 axis rows + path. The CSS `.cx-axes` is a flex/grid container in `globals.css` that auto-reflows; no styling change needed. Keyboard `1`/`2` still pick proposals on the active unresolved axis (Type or From).

    Do NOT remove the AxisGroup component or its tests — it's still rendered for Type and From.
  </action>
  <verify>
    <automated>npx tsc --noEmit && rg --type tsx --type ts "Context|axis_context|contexts" components/triage/ app/\(app\)/taxonomy/ ; echo 'inspect: only commentary/CSS-class-name uses should remain'</automated>
  </verify>
  <done>
    `npx tsc --noEmit` exits 0.
    Triage page in dev (`npm run dev`) renders the active label-stage card with exactly 2 axis rows (Type, From) — no Context row.
    Taxonomy page renders 2 tabs (Types, Entities). Clicking each shows the corresponding label list.
    Keyboard `1`/`2` pick proposals; pressing `3` does nothing in label mode.
    The mutation POST body to `/api/triage` carries `picks: { Type, From }` only — no `Context` key (visible in browser devtools network tab).
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Jest test fixtures + assertions across __tests__/ and agent/__tests__/ to match the two-axis contract</name>
  <files>
    __tests__/classify-api.test.ts,
    __tests__/classify-auto-actions.test.ts,
    __tests__/triage-api.test.ts,
    __tests__/labels-samples-api.test.ts,
    __tests__/taxonomy-internal-api.test.ts,
    agent/__tests__/consumer-prompts.test.ts,
    agent/__tests__/consumer-stage2-prompt.test.ts,
    agent/__tests__/consumer-stage2.test.ts
  </files>
  <action>
    For every test file listed below, update fixtures and assertions to drop the context axis. The pattern is mechanical: any literal `context: { value, confidence }` block inside an `axes` object → DELETE; any `axis_context: <value>` inside `data` updates → DELETE; any `axis_context_confidence` → DELETE; any `context: ['...']` inside a TaxonomyContext / TaxonomyInternalResponse fixture → DELETE.

    **__tests__/classify-api.test.ts** (12 axis_context refs across 5+ test cases):
    - In every Stage 2 success request body fixture, drop `context: { value: ..., confidence: ... }` from `axes`.
    - In every assertion `expect(updateArg.data.axis_context).toBe(...)` and `expect(updateArg.data.axis_context_confidence).toBe(...)`: DELETE the line. (Don't replace with `.toBeUndefined()` — that's noise.)
    - Update the auto-file gate test: it asserted all 3 confidences ≥ 0.85 → now asserts both confidences ≥ 0.85.
    - Add ONE new minimal test verifying that a Stage 2 body with `axes.context` present is rejected as parse_error → 400. (TDD-justified: this is the core contract change. Insert near the existing AxisSchema-rejects-null-with-high-conf test.)

    **__tests__/classify-auto-actions.test.ts** (handful of `context: { value, confidence }` and `axis_context` refs):
    - Same deletions as above. Auto-file/auto-ignore gates still verify with type+from confidence math.
    - The `expect(updateArg.data.axis_context).toBe('paid')` line at ~144 → DELETE.
    - Any test that verified "all 3 axes ≥ 0.85 fires auto-file" reframes as "both axes ≥ 0.85 fires auto-file".

    **__tests__/triage-api.test.ts** (1 ref at line 220 — `axis_context: 'FonnIT/Clients'`):
    - Drop `axis_context: 'FonnIT/Clients'` from the prisma.item mock fixture. The triage list response normalisation should no longer carry that field through.
    - If any POST test passes `picks.Context`, switch to `picks.Type` + `picks.From` only. Add a tiny case verifying that POST with `picks: { Context: 'x' }` does NOT create a TaxonomyLabel for axis='context' (Zod rejects the unknown key under strict parse, OR the picks?.Context branch was deleted and the `data` block stays clean — assert no `axis_context` key in the prisma.item.update `data` arg).

    **__tests__/labels-samples-api.test.ts** (Test 5c at line 117 — `axis=context filters on axis_context`):
    - DELETE Test 5c entirely (the route now rejects `axis=context` → 400).
    - Replace with a new assertion: a request to `?axis=context&label=anything` returns status 400. (This stays in the test file — the route's whitelist guard is the new contract, and this is the test for it.)
    - In any other fixture using `axis_context: 'finance'` or `axis_context: null` in the row mock for a `axis=type` or `axis=from` test: those are inert (the SELECT no longer projects `axis_context`). Drop them for cleanliness — a stale field won't fail the test, but the diff is clearer if removed.

    **__tests__/taxonomy-internal-api.test.ts** (lines 73–74 — `{ axis: 'context', name: 'finance-monthly' }`, `{ axis: 'context', name: 'travel' }`):
    - DELETE the two `{ axis: 'context', ... }` rows from the prisma.taxonomyLabel.findMany mock return value.
    - The expected response shape changes from `{ type, from, context }` to `{ type, from }`. Update any `expect(json.context).toEqual(...)` line: DELETE. Update `expect(json).toEqual({ type: [...], from: [...], context: [...] })` to `expect(json).toEqual({ type: [...], from: [...] })`.

    **agent/__tests__/consumer-prompts.test.ts** (lines 52, 187, 237 — `context: ['finance-monthly', 'travel']`, etc.):
    - In every `TaxonomyContext` fixture passed to `buildStage2Prompt`: drop the `context: [...]` field. The TaxonomyContext type no longer has it; the test will fail to compile if you don't.
    - Drop any assertion that scans the rendered prompt for `Context axis:` — those should now assert ABSENCE: `expect(prompt).not.toContain('Context axis')`.
    - The "200-element context array doesn't blow up the prompt" test at line 237: rephrase the test or DELETE if it specifically validates context-axis fan-out (the type/from arrays already cover that property).

    **agent/__tests__/consumer-stage2-prompt.test.ts** (11 refs across multiple test cases):
    - Every `axes: { type: ..., from: ..., context: { value, confidence } }` fixture for the LLM mock: DROP the `context: { ... }` block.
    - Every `Stage2ResultSchema`-style fixture inline: DROP context.
    - Tests that asserted `payload.axes.context` is forwarded to the classify body: DELETE those expectations. Replace with `expect(payload.axes).not.toHaveProperty('context')`.

    **agent/__tests__/consumer-stage2.test.ts** (5 refs):
    - `EMPTY_TAXONOMY: TaxonomyInternalResponse = { type: [], from: [], context: [] }` → drop `context: []`.
    - Two-axis fixture for the success path: drop the `context: { value: 'finance', confidence: 0.85 }` block.
    - Type signatures inside the test (e.g., `axes: { type: ...; from: ...; context: ... }`) — drop `context`.

    No new tests for "the strip" itself beyond the two contract-edge assertions called out above (classify rejects axes.context; labels/samples rejects axis=context). Per the constraints in the planning brief: TDD-first only on routes with existing tests; do not write new tests purely to "test the strip".

    Run `npx jest` after edits — expect green modulo the 6 pre-existing failures noted in STATE.md (out-of-scope work tracked as the Stage 2 acceptance work). If a test fails for an unrelated reason (drift uncovered while editing), STOP and surface it before continuing — don't paper over it.
  </action>
  <verify>
    <automated>npx jest --silent 2>&1 | tail -40 ; cd agent && npx jest --silent 2>&1 | tail -20</automated>
  </verify>
  <done>
    `npx jest` exits 0 (or only the documented pre-existing failures, which Daniel will sign off on).
    `(cd agent && npx jest)` exits 0.
    Two new edge-case assertions added: classify rejects `axes.context`, labels/samples rejects `axis=context`.
    No test still references `axis_context`, `axis: 'context'`, or `context: { value, confidence }` in any axes object.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks land:

1. **Type-check**:
   `npx tsc --noEmit` → exit 0
   `(cd agent && npx tsc --noEmit)` → exit 0

2. **Tests**:
   `npx jest` → green (modulo pre-existing failures noted in STATE.md)
   `(cd agent && npx jest)` → green

3. **Static grep** (the canonical "Decision 1 done" check from the planning brief):
   `rg "axis_context|axis: 'context'|\"contexts\"" app/ agent/src/ components/ lib/`
   → returns NO matches.
   `rg "axis_context" prisma/`
   → returns the 2 schema column lines (kept by spec).

4. **End-to-end smoke** (Daniel runs locally):
   - Reset local DB, apply seed, drop one new file in Downloads.
   - Tail `claude -p` stdout (process-files.ts logs); confirm the FINAL JSON has no `context` key in `axes`.
   - Confirm `SELECT axis_context FROM "Item" WHERE id = '<new>'` is `null`.
   - Open `/triage` — confirm card shows Type + From only, no Context row.
   - Hit `curl -H "Authorization: Bearer $CORTEX_API_KEY" 'http://localhost:3000/api/labels/samples?axis=context&label=anything'` → 400.
   - Open `/taxonomy` — confirm two tabs only.

5. **Embedding sanity**:
   - The cron/embed route still composes useful embed text from filename + type + from + subject. Verified by `buildEmbedText` returning a non-empty `' | '`-joined string for any item with at least one of those fields. No re-embedding of historical items needed (column data preserved; we just stop adding context to NEW embed texts).
</verification>

<success_criteria>
- Decision 1 from SEED-v4-prod.md is fully shipped at runtime.
- The schema column `axis_context` is preserved with no migration.
- Stage 2 no longer elicits, persists, or surfaces axis_context.
- The triage UI and taxonomy page no longer mention Context.
- 3 atomic commits land in order: (1) backend strip, (2) UI strip, (3) test-fixture updates.
- TS clean; Jest green (modulo pre-existing failures).
- No straggler `axis_context` / `axis: 'context'` / `"contexts"` references in `app/`, `agent/src/`, `components/`, `lib/`.
</success_criteria>

<output>
After completion, create `.planning/quick/260430-g6h-finish-decision-1-drop-axis-context-thro/260430-g6h-SUMMARY.md` with:
- 3 commit SHAs
- Files changed per task
- Smoke-test transcript snippet showing Stage 2 JSON output without `context`
- Confirmation that prisma/schema.prisma is untouched
- STATE.md update (move to "Quick Tasks Completed" table row, append commit SHAs)
</output>
